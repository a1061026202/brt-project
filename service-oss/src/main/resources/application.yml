server:
  port: 8130 # 服务端口

spring:
  profiles:
    active: dev
  application:
    name: service-oss

  cloud:
    nacos:
      discovery:
        server-addr:  # nacos服务地址



txy:
  oss:
    endpoint: 
    secret-id: 
    bucket-name: 
    secret-key: 
